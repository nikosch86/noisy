services:
  noisy:
    # Pull the published multi-arch image:
    image: ghcr.io/nikosch86/noisy:latest
    # Or build from source:
    # build:
    #   context: ../../
    command: --config config.json
